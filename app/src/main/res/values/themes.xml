<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lightGray</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="myText" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/normalTextSize</item>
    </style>

    <style name="myText.blackAndBold" parent="Widget.AppCompat.TextView">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="myText.darkBlueAndBold" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/darkBlue</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="myText.white" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="myText.lightGray" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/lightGray</item>
    </style>

    <style name="myText.lightBlue" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/lightBlue</item>
    </style>
    <style name="roundedImage">
        <item name="cornerSize">8dp</item>
    </style>
</resources>